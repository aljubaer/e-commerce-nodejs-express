export const BASE_URI = `http://localhost:5000`;

export const LOGIN_URI = `${BASE_URI}/login`;

export const REGISTER_URI = `${BASE_URI}/register`;

export const PRODUCT_URL = `${BASE_URI}/products`;

export const WISHLIST_URL = `${BASE_URI}/wishlist`;
